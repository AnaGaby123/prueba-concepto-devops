const typeApi = 'replace-purchase-order-item';
/*
export const FETCH_QUOTES_CLIENT = createAction(
  buildingStringActionType(typeApi, 'Fetch Quotes of Client'),
);
export const SET_QUOTED_SELECTED = createAction(
  buildingStringActionType(typeApi, 'Guardar la cotización seleccionada'),
  props<{item: IQuoted}>(),
);
export const FETCH_QUOTED_ITEMS_LOAD = createAction(
  buildingStringActionType(typeApi, 'Fetch Quote Items'),
);
export const FETCH_LIST_QUOTED_ITEMS_LOAD = createAction(
  buildingStringActionType(typeApi, 'Fetch List Quote Items'),
);
export const FETCH_PRODUCTS_CATALOG_LOAD = createAction(
  buildingStringActionType(typeApi, 'Fetch Product Catalog Load'),
);
export const SET_ITEM_LIST_CATALOG = createAction(
  buildingStringActionType(typeApi, 'Set Items of Catalogs'),
  props<{items: Array<IQuoteItem>}>(),
);
export const SET_QUOTES_CLIENT_SUCCESS = createAction(
  buildingStringActionType(typeApi, 'Obtener cotizaciones del cliente Exitosamente'),
  props<{quoteList: Array<IQuoted>}>(),
);
export const FETCH_QUOTE_ITEM_LIST_LOAD = createAction(
  buildingStringActionType(typeApi, 'Fetch Quote Items'),
);
export const FETCH_QUOTED_ITEMS_SUCCESS = createAction(
  buildingStringActionType(typeApi, 'Fetch Quote Items Success'),
  props<{items: Array<IQuoteItem>}>(),
);
export const FETCH_LIST_QUOTED_ITEMS_SUCCESS = createAction(
  buildingStringActionType(typeApi, 'Fetch List Quote Items Success'),
  props<{list: Array<IQuoteItem>}>(),
);
export const FETCH_PRODUCT_CATALOG_LOAD = createAction(
  buildingStringActionType(typeApi, 'Fetch Product Catalog Load'),
);
export const SET_SELECT_ITEM = createAction(
  buildingStringActionType(typeApi, 'Set Item Selected'),
  props<{item: IQuoteItem; value: boolean}>(),
);
export const UPDATE_QUOTE_SELECTED = createAction(
  buildingStringActionType(typeApi, 'Update quoted selected'),
  props<{idQuoted: string}>(),
);
export const SET_RUN_SEARCH_TERM = createAction(
  buildingStringActionType(typeApi, 'Set Run Search Term'),
  props<{searchTerm: string}>(),
);
export const SET_QUOTES_CLIENT_OF_SEARCH = createAction(
  buildingStringActionType(typeApi, 'Set Values of Search'),
  props<{product: DropListOption}>(),
);
export const SET_TYPE_FILTER_SEARCH = createAction(
  buildingStringActionType(typeApi, 'Set Type Of Filter Search'),
  props<{filterType: DropListOption}>(),
);
export const GET_OPTIONS_OF_PRODUCTS = createAction(
  buildingStringActionType(typeApi, 'Get Options of Product'),
  props<{searchTerm: string}>(),
);
export const GET_OPTIONS_OF_PRODUCTS_SUCCESS = createAction(
  buildingStringActionType(typeApi, 'Save Products of Search'),
  props<{product: Array<SugerenciaBusqueda>}>(),
);
export const GET_OPTIONS_OF_PRODUCTS_ERROR = createAction(
  buildingStringActionType(typeApi, 'Save Products of Search Error'),
);
export const REPLACE_ITEM_LOAD = createAction(
  buildingStringActionType(typeApi, 'Replace item of Order'),
);
export const REPLACE_ITEM_SUCCESS = createAction(
  buildingStringActionType(typeApi, 'Replace Success'),
);
export const REPLACE_ITEM_ERROR = createAction(
  buildingStringActionType(typeApi, 'Replace Success'),
);
*/
